# main.py

"""
        airflow

        dag задачи 1:

        ELT
            E - выгрузка свечей c api
            L - загрузка в ленточную БД
            T - расчеты ma rsi sma -> витрины представления

        dag задачи 2:
        расчет коэфициентов
            выгрузка из БД история свечей
            скрипт подбора опитмальных коэфов
            загрузка в БД

        внешнее api - один запрос один ответ - отдельный скрипт


    все это обернуто в docker для быстрого развертывания на сервере

"""


'''

ассинхронные запросы по каждому таймфрему и символу с целью получить свечи

пропускаем запись в kafka

запись в БД

подсчет показателей с сохранением в витрине


## Архитектура проекта (Airflow + Docker)

Проект построен вокруг **Apache Airflow** как оркестратора задач, упакован
в **Docker** для унификации окружения и лёгкого развертывания на сервере.

### Общая схема данных

1. **Источник** — внешнее API (Coinbase).
2. Producer Kafka - накапливает данные с биржи
3. **Хранилище** — ленточная (стриминговая) база данных (предположительно
PostgreSQL), куда загружаются сырые данные.
4. **Витрины** — агрегированные таблицы (расчёты MA, RSI, SMA) для аналитики.
5. **Оптимизация** — отдельный процесс подбора коэффициентов, использующий
исторические данные из БД и сохраняющий результаты обратно.

---

### Компоненты

#### 1. Внешнее API
- Реализовано как отдельный Python-модуль (`coinbase_api.py`).
- Отвечает за извлечение данных **по запросу** (один запрос → один ответ).
- Может использоваться как внутри DAG, так и независимо.

#### 2. База данных (ленточная)
- Служит для долговременного хранения сырых свечей (таблица `raw_candles`).
- Поддерживает эффективную запись больших объёмов данных (предположительно TimescaleDB).
- Из неё строятся витрины и читаются данные для оптимизации.

#### 3. Airflow DAGs
Две основные задачи (DAG), которые могут запускаться по расписанию или вручную.

---

### DAG 1: ELT-конвейер

**Цель:** загрузить новые свечи из API, сохранить их в БД и рассчитать индикаторы.

| Этап | Описание |
|------|----------|
| **E (Extract)** | Вызов внешнего API для получения свечей за требуемый период (инкрементально). |
| **L (Load)** | Загрузка полученных данных в таблицу `raw_candles` (сырые данные, минимальная обработка). |
| **T (Transform)** | Внутри БД выполняются SQL-запросы для расчёта MA, RSI, SMA. Результаты складываются
в витрины (`mv_indicators`, `daily_stats` и т.д.). |

**Особенности:**
- Используется **ELT**, а не ETL: трансформация происходит в БД, что ускоряет итерации и снижает нагрузку на Python.
- Применяется триггерная загрузка (по расписанию или после появления новых данных).

---

### DAG 2: Оптимизация коэффициентов

**Цель:** на основе исторических свечей подобрать оптимальные параметры
модели (например, для торговой стратегии).

| Этап | Описание |
|------|----------|
| **Выгрузка** | Чтение исторических свечей из БД (витрин или `raw_candles`). |
| **Оптимизация** | Запуск скрипта на Python (мультипроцессорный, адаптивный поиск), который перебирает диапазоны коэффициентов и оценивает метрики. |
| **Сохранение** | Результаты (лучшие коэффициенты, метрики) записываются обратно в БД в таблицу `optimization_results`. |

**Примечание:** этот DAG  запускается реже (например, раз в час/день).

---

### Docker-упаковка

Всё окружение (Airflow, Python-скрипты, база данных) описано в `docker-compose.yml`. Это позволяет:

- Быстро развернуть проект на любом сервере.
- Иметь изолированные версии зависимостей.
- Легко масштабировать (например, добавить воркеров Airflow).

**Типовая структура:**
- `docker-compose.yml` — описание сервисов (airflow-webserver, scheduler, postgres, redis, и т.д.).
- `Dockerfile` для кастомизации образа Airflow (установка дополнительных Python-пакетов, копирование кода).
- Папка `dags/` — размещение DAG-файлов.
- Папка `scripts/` — утилиты (внешнее API, оптимизатор).

'''
